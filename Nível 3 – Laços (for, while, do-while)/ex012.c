//Imprima os números pares de 0 até 100.
#include <stdio.h>
int main() {
    int i;

    // Loop para imprimir os números pares de 0 a 100
    for (i = 0; i <= 100; i += 2) {
        printf("%d\n", i);
    }

    return 0;
}